meta {
  name: Route Set Enable Status
  type: http
  seq: 10
}

post {
  url: {{url}}/api/v1/routes/{{api_proxy_name}}/{{route_id}}/status
  body: json
  auth: none
}

body:json {
  {
    "enabled": true
  }
}
